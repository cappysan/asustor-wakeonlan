# Functions
# =========
function logger() {
  echo "${@}" >&2
  syslog --log 0 --level 0 --user SYSTEM --event "${@}"
}

WHAT=Wakeonlan

# User
# ====
export APKG_USER=admin
export APKG_GROUP=root
export HOME=/share/Configuration/wakeonlan

# vim: ft=sh
